"""Participant algorithms — contestant implementations discovered by scanning.

Each subdirectory matches a scenario id (e.g. ``search_track/``); each ``.py``
file inside is one algorithm. The platform scans this tree with static AST
analysis (no import/execution) and lists them in the UI. See README.md.
"""
