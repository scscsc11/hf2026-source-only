export declare const OnboardingStorage: {
    /** 是否已完成引导(读过或跳过)。localStorage 不可用时返回 false。 */
    isCompleted(): boolean;
    /** 标记引导已完成。localStorage 不可用时静默失败。 */
    markCompleted(): void;
};
//# sourceMappingURL=onboarding-storage.d.ts.map